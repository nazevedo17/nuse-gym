import styles from 'styles/components/Header.module.css';

const Header = ({}) => (
  <header>
    <nav>
      <ul>
        <li>Logo</li>
        <li>Login</li>
      </ul>
    </nav>
  </header>
);

export default Header;
